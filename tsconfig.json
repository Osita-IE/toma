{
  "compilerOptions": {
    "strict": true,
    "jsx": "react-jsx",                      // 👈 tells TS to use React's JSX
    "types": ["react", "react-native"],      // 👈 makes JSX namespace available
    "lib": ["esnext"],                       // 👈 ensures latest JS features
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts",
    "nativewind-env.d.ts"
  ],
  "extends": "expo/tsconfig.base"
}
